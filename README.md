# 🤖 RoboPlay - منصة تعليم تفاعلية لهندسة الذكاء الاصطناعي

منصة تعليمية شاملة وتفاعلية لتعليم البرمجة والذكاء الاصطناعي بطريقة ممتعة وفعالة، مستوحاة من تطبيقات التعليم الحديثة مثل Duolingo.

## ✨ الميزات الأساسية

### 🎓 نظام المستخدمين المتقدم
- تسجيل دخول واشتراك آمن
- ملفات شخصية مع تتبع التقدم الشامل
- اختبار تحديد المستوى المبدئي
- نظام تحفيز متقدم (XP، السترياك، القلوب)

### 📚 الهيكل التعليمي المنظم
- مسارات تعليمية منفصلة (Python, C++, MATLAB)
- مشاريع عملية (روبوت، رؤية حاسوبية، شبكات عصبية)
- نظام Duolingo (سترياك، قلوب، نقاط، شرائط تقدم)
- شهادات بعد كل مسار

### 💻 محاكيات مدمجة متقدمة
- محرر كود مباشر في المتصفح (Monaco Editor)
- تشغيل Python مباشرة (Pyodide)
- دمج Jupyter Notebooks
- دعم لغات برمجة متعددة

### 🏆 نظام التقييم والشهادات
- تقييم المشاريع العملية
- نقاط XP تراكمية
- شهادات قابلة للتحميل
- نظام الترتيب (Leaderboard)

## 🛠️ المتطلبات التقنية

### Frontend
- **React 19** - مكتبة واجهة المستخدم
- **Vite** - أداة البناء السريعة
- **React Router** - نظام التوجيه
- **Tailwind CSS** - تصميم الواجهات
- **Zustand** - إدارة الحالة
- **Axios** - طلبات HTTP
- **Lucide React** - الأيقونات

### Backend
- **Express.js** - خادم الويب
- **MongoDB** - قاعدة البيانات
- **JWT** - المصادقة
- **Bcryptjs** - تشفير كلمات المرور

## 📁 هيكل المشروع

```
roboplay/
├── src/                    # كود الواجهة الأمامية
│   ├── components/        # مكونات React
│   ├── pages/            # صفحات التطبيق
│   ├── store.ts          # إدارة الحالة (Zustand)
│   ├── types.ts          # أنواع TypeScript
│   ├── const.ts          # الثوابت والألوان
│   ├── App.jsx           # المكون الرئيسي
│   └── main.jsx          # نقطة الدخول
├── server/               # كود الخادم الخلفي
│   ├── index.js          # الخادم الرئيسي
│   ├── .env              # متغيرات البيئة
│   └── package.json      # مكتبات الخادم
├── public/               # الملفات الثابتة
├── vite.config.js        # إعدادات Vite
├── tailwind.config.js    # إعدادات Tailwind
├── postcss.config.js     # إعدادات PostCSS
├── package.json          # مكتبات الواجهة الأمامية
└── README.md            # هذا الملف
```

## 🚀 البدء السريع

### 1. تثبيت المتطلبات
```bash
# تثبيت مكتبات الواجهة الأمامية
npm install

# تثبيت مكتبات الخادم
cd server && npm install && cd ..
```

### 2. إعداد قاعدة البيانات
```bash
# تأكد من تشغيل MongoDB
# يمكنك استخدام MongoDB Atlas أو MongoDB محليًا
```

### 3. تشغيل التطبيق

#### تشغيل الخادم الخلفي
```bash
cd server
npm start
# الخادم سيعمل على http://localhost:3000
```

#### تشغيل الواجهة الأمامية (في نافذة طرفية جديدة)
```bash
npm run dev
# الواجهة الأمامية ستعمل على http://localhost:5173
```

## 📊 قاعدة البيانات

### نماذج البيانات الرئيسية

#### User (المستخدم)
```javascript
{
  id: ObjectId,
  name: String,
  email: String,
  password: String (hashed),
  level: Number,
  streak: Number,
  xp: Number,
  hearts: Number,
  totalXP: Number,
  createdAt: Date,
  updatedAt: Date
}
```

#### Course (الدورة)
```javascript
{
  id: ObjectId,
  title: String,
  description: String,
  language: String,
  level: 'beginner' | 'intermediate' | 'advanced',
  icon: String,
  lessons: [ObjectId],
  createdAt: Date
}
```

#### Lesson (الدرس)
```javascript
{
  id: ObjectId,
  courseId: ObjectId,
  title: String,
  description: String,
  content: String,
  order: Number,
  exercises: [ObjectId],
  createdAt: Date
}
```

#### Exercise (التمرين)
```javascript
{
  id: ObjectId,
  lessonId: ObjectId,
  title: String,
  description: String,
  type: 'code' | 'quiz' | 'project',
  initialCode: String,
  expectedOutput: String,
  xpReward: Number,
  difficulty: 'easy' | 'medium' | 'hard',
  testCases: Array,
  createdAt: Date
}
```

## 🎨 نظام الألوان

| اللون | الكود | الاستخدام |
|------|------|-----------|
| أزرق | #3498db | اللون الأساسي |
| برتقالي | #e67e22 | اللون الثانوي |
| أصفر | #f1c40f | اللون التركيزي |
| أخضر | #2ecc71 | اللون الإيجابي |
| أحمر | #e74c3c | اللون السلبي |

## 📝 الصفحات الرئيسية

### 1. صفحة التسجيل والدخول (Auth)
- تسجيل حساب جديد
- تسجيل دخول
- التحقق من البيانات

### 2. اختبار تحديد المستوى (Assessment)
- 5 أسئلة لتحديد مستوى المستخدم
- تحديد اللغة المفضلة
- تحديد الأهداف التعليمية

### 3. لوحة التحكم (Dashboard)
- عرض إحصائيات المستخدم (XP، السترياك، القلوب، المستوى)
- عرض المسارات التعليمية المتاحة
- الانتقال إلى الدروس

### 4. صفحة الدرس (Lesson)
- عرض محتوى الدرس
- محرر الكود المدمج
- تنفيذ الكود وعرض النتائج
- إرسال التمارين والحصول على XP

## 🔐 الأمان

- تشفير كلمات المرور باستخدام bcryptjs
- استخدام JWT للمصادقة
- CORS مفعل للحماية من الطلبات غير المصرح بها
- التحقق من الإدخال على الخادم

## 📈 الخطوات التالية

- [ ] تطوير محرر الكود المتقدم
- [ ] دمج Pyodide لتنفيذ Python
- [ ] إضافة نظام الترتيب (Leaderboard)
- [ ] إضافة نظام الإشعارات
- [ ] تطوير تطبيق الهاتف المحمول
- [ ] إضافة مزيد من المسارات التعليمية

## 📞 الدعم

للمساعدة والدعم، يرجى فتح issue في المستودع.

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License.

---

**تم الإنشاء بـ ❤️ لتعليم الذكاء الاصطناعي والبرمجة**
